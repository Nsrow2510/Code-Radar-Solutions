#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);
    if (a >= 'A' && a <= 'Z') {
        printf("Uppercase");
    } else if(a>='a' && a<='z') {
        printf("Lowercase");
    }

    return 0;
}