#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);
    if (a >= 'A' && a <= 'Z') {
        printf("Uppercase");
    } else {
        printf("Lowercase");
    }

    return 0;
}