#include <stdio.h>

int main() {
    char n;
    scanf("%c", &n);
    if (a >= 'A' && a <= 'Z') {
        printf("Uppercase");
    } else if(a>='a' && a<='z') {
        printf("Lowercase");
    }

    return 0;
}