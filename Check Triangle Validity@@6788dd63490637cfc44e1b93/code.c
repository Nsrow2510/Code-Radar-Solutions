#include <stdio.h>

int main() {
    int n = 2;
    int a, b, c;
    scanf("%d %d %d", &a, &b, &c);
    if (pow(a, n) + pow(b, n) = c) {
        printf("Valid");
    } else {
        printf("Invalid");
    }

    return 0;
}