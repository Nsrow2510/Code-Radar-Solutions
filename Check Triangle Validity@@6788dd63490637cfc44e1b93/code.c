#include <stdio.h>

int main() {
    int a, b, c;
    scanf("%d %d %d", &a, &b, &c);
    if (pow(a, 2) + pow(b, 2) = c) {
        printf("Valid");
    } else {
        printf("Invalid");
    }

    return 0;
}